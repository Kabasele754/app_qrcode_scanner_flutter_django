import 'package:flutter/widgets.dart';
import 'package:provider/provider.dart';

class CretePage extends StatelessWidget {
  const CretePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
